name: CI

on:
  push:
    branches:
      - main
      - 'marc/**'
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm test

  build-windows:
    runs-on: windows-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run make
      - name: Generate SHA256 hashes
        shell: pwsh
        run: |
          Get-ChildItem -Path out/make -Recurse -File |
            Where-Object { $_.Name -notlike '*.blockmap' } |
            ForEach-Object {
              $hash = Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName
              "$($hash.Hash.ToLower())  $($_.FullName.Substring((Resolve-Path out/make).Path.Length + 1).Replace('\', '/'))"
            } | Set-Content -Encoding ASCII SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: windows-x64-artifacts
          path: |
            out/make/**
            SHA256SUMS.txt
